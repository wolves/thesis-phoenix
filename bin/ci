#!/usr/bin/env bash

echo Running tests...

mix deps.get && mix test && npm install && npm test &&
cd examples/example-phx-1_2 && mix deps.get && mix test &&
cd ../example-phx-1_3 && mix deps.get && mix test &&
npm install && npm install -g phantomjs-prebuilt && npm test &&
cd -
